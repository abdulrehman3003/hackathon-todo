# Animations CSS Spec (animations.css)

Keyframes:
- fade-in (0 to 1 opacity)
- slide-up (translateY(8px) -> 0)
- spin (for loader)
Transitions:
- default transition for interactive elements: transform 160ms ease, opacity 200ms
