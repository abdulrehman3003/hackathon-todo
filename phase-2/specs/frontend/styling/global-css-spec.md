# Global CSS Spec (global.css)

Responsibilities:
- normalize CSS (box-sizing, body margins)
- import variables.css
- set base font, background, and color
- define helper classes: .container, .sr-only

Responsive rules:
- set breakpoints mobile/tablet/desktop
- utility spacing classes allowed but minimal

Prefer semantic class names (no utility-first approach)
